# Salle de réunion — Variables d'environnement

# Daily.co API key (optionnelle — room publique, join sans token)
# Utile pour le endpoint GET /api/daily qui vérifie l'état de la room
# https://dashboard.daily.co/
DAILY_API_KEY=
